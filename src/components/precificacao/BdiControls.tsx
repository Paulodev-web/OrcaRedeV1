"use client";

/**
 * @deprecated Substituído por `TaxInput` no novo modelo de Margem de Contribuição.
 * Mantido como stub neutro para preservar histórico do arquivo.
 * Não é mais renderizado pelo `PrecificacaoCalculator`.
 */
export function BdiControls() {
  return null;
}

"use client";

/**
 * @deprecated Substituído por `CostItemsTable` no novo modelo de Margem de Contribuição.
 * A consolidação dos materiais do orçamento agora alimenta diretamente a Receita Bruta.
 * Mantido como stub neutro para preservar histórico do arquivo.
 * Não é mais renderizado pelo `PrecificacaoCalculator`.
 */
export function PricingMaterialsTable() {
  return null;
}

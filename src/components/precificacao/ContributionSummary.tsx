"use client";

/**
 * @deprecated Substituído por `ServicePricingSummary` no novo modelo de Serviço + Materiais.
 * Mantido como stub neutro para preservar histórico do arquivo.
 * Não é mais renderizado pelo `PrecificacaoCalculator`.
 */
export function ContributionSummary() {
  return null;
}

"use client";

/**
 * @deprecated Mão de obra agora é inserida como um `CostItem` genérico em `CostItemsTable`.
 * Mantido como stub neutro para preservar histórico do arquivo.
 * Não é mais renderizado pelo `PrecificacaoCalculator`.
 */
export function LaborInput() {
  return null;
}
